<div class="container"></div>
    <div class="row">
        {{#each cars}}
        {{>carElement}}
        {{/each}}
    </div>
</div>

{{!-- <section id="viewCatalog" class="background-img">
    <div class="added-cars">
        {{#each cars}}
        {{>carElement}}
        {{/each}}
    </div>
</section> --}}