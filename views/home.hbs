<div class="container mb-3 mt-3"></div>
<div class="row">
    {{#each cars}}
    {{>carElement}}
    {{/each}}
</div>
</div>

{{!-- <section id="viewCatalog" class="background-img">
    <div class="added-cars">
        {{#each cars}}
        {{>carElement}}
        {{/each}}
    </div>
</section> --}}